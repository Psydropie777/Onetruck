@model IEnumerable<IdentitySample.Models.TruckModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TruckBrand.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TruckBrand.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.truckModelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.truckModelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.truckModelId })
        </td>
    </tr>
}

</table>
